import React from "react";
import { NavLink, NavContainer } from "../navbar/NavBar.styles";
const NavBar = () => {
  return (
    <NavContainer>
      <NavLink to="/">🏠</NavLink>
    </NavContainer>
  );
};

export default NavBar;
